<%= form_for(campodiagnostico) do |f| %>
  <% if campodiagnostico.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(campodiagnostico.errors.count, "error") %> prohibited this campodiagnostico from being saved:</h2>

      <ul>
      <% campodiagnostico.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tipo_grupo %>
    <%= f.text_field :tipo_grupo %>
  </div>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="field">
    <%= f.label :valor %>
    <%= f.text_field :valor %>
  </div>

  <div class="field">
    <%= f.label :fotodiagnostico_id %>
    <%= f.collection_select :fotodiagnostico_id, Paciente.all, :id, :nombre %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
