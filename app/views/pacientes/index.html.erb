<%= render 'shared/sidebar/pacientes' %>
<div class="oro-main-ui container-fluid">
  <div class="oro-main__actions">
    <div class="btn-group pull-right">
      <%if @pacientes.count == 0 %>
        <div class="oro-help oro-help--nuevo-paciente">
          <p class="oro-help__texto">
            Crear tu primer paciente
          </p>
        </div>
      <% end %>
      <form action="/pacientes/new" >
        <input type="submit" value="Nuevo Paciente" class="btn btn-default">
      </form>
    </div>
  </div>
  <br>
<%if @pacientes.count > 0 %>
    <div class="oro-paciente__list hidden">
      <% @pacientes.each do |paciente| %>
        <div class="oro-paciente__card">
          <div class="oro-paciente__perfil">
            <div class="oro-paciente__media">
              <img src="" alt="<%= paciente.nombre %>" class="oro-paciente__image">
            </div>
            <strong class="oro-paciente__name">
              <%= paciente.nombre %> <%= paciente.apellido %>
            </strong>
            <small class="oro-paciente__detail">
              <%= paciente.correo %>
            </small>
          </div>
          <div class="oro-paciente__message">

          </div>
          <div class="oro-paciente__options">
            <div class="btn-group">
              <%= link_to 'Ver', paciente,class: 'btn'  %>
              <%= link_to 'Editar', edit_paciente_path(paciente), class: 'btn' %>
              <%= link_to 'Eliminar', paciente, method: :delete, data: { confirm: '¿Estás seguro?' },class: 'btn'  %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
    <div class="oro-paciente__list oro-paciente__list--table">
      <div class="table-resposive">
        <table class="table">
          <thead>
          <tr>
            <th>Nombre</th>
            <th>Apellido</th>
            <th>Documento</th>
            <th>Celular</th>
            <th>Teléfono</th>
            <th>Email</th>
            <th colspan="3"></th>
          </tr>
          </thead>

          <tbody>
          <% @pacientes.each do |paciente| %>
            <tr>
              <td><%= paciente.nombre %></td>
              <td><%= paciente.apellido %></td>
              <td><%= paciente.documento %></td>
              <td><%= paciente.celular %></td>
              <td><%= paciente.telefono %></td>
              <td><%= paciente.correo %></td>
              <td><%= link_to 'Ver', paciente %></td>
              <td><%= link_to 'Editar', edit_paciente_path(paciente) %></td>
              <td><%= link_to 'Eliminar', paciente, method: :delete, data: { confirm: '¿Estás seguro?' } %></td>
            </tr>
          <% end %>
          </tbody>
        </table>
      </div>
    </div>
<% end %>

  <script type="text/javascript" charset="utf-8">
      $('.oro-subnavbar__link').removeClass('oro-subnavbar__link--active')
      $('#nav-pacientes').addClass('oro-subnavbar__link--active')
  </script>
<br>
</div>
